<Window
    xmlns:controls="clr-namespace:Avalonia.Controls;assembly=Avalonia.Controls" xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="http://furesoft.ml/expay"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="ExPay_Service.Dialogs.PayDialog"
        Title="PayDialog" Height="500" Width="450" WindowStartupLocation="CenterScreen">
  <Grid RowDefinitions="50,*,50">
    <StackPanel Grid.Row="0">
      <TextBlock Text="{Binding MerchantInfo.Name}" />
    </StackPanel>

    <ContentControl Grid.Row="1" x:Name="content" />

    <Grid Grid.Row="2">
      <Button Click="OnCancel" MinWidth="125" HorizontalAlignment="Left" Content="Cancel" />

      <Button Click="OnPay" Content="Pay" MinWidth="125" HorizontalAlignment="Right" />
    </Grid>
  </Grid>
</Window>